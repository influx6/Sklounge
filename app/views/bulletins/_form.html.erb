
<% form_for @bulletin do |f| %>
  <% if @bulletin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bulletin.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @bulletin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <br/>
  <p>
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </p>
  <br/>
  <p><%= f.submit %></p>
<% end %>

